//! skilloom — a TUI-first manager for AI agent skills.
//!
//! This is a scaffold. The engine, the TUI, and the headless `--json` surface
//! are specified documentation-first under `docs/` and built during the
//! planning and implementation phases.
//!
//! Start here: `docs/adrs/0003-skilloom-engine-design-and-scope.md`.

fn main() {
    println!("skilloom — AI skill manager (scaffold, not yet implemented)");
    println!(
        "See docs/ for the design. The engine, TUI, and `--json` surface are still to be built."
    );
}
